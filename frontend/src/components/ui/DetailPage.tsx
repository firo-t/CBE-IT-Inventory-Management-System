'use client';import {useEffect,useState} from 'react';import {api} from '@/lib/api';import Page from './Page';import Badge from './Badge';export default function DetailPage({title,endpoint,fields}:any){const [d,setD]=useState<any>();const [err,setErr]=useState('');useEffect(()=>{api.get(endpoint).then(r=>setD(r.data)).catch(e=>setErr(e.response?.data?.message||'Unable to load record.'))},[endpoint]);return <Page title={title} description="Record details" showBack={true}><div className="card">{err&&<div className="alert">{err}</div>}{!d&&!err&&<div>Loading...</div>}{d&&<div className="detail-grid">{fields.map((f:any)=><div className="kv" key={f.key}><small>{f.label}</small>{f.badge?<Badge value={d[f.key]}/>:<b>{d[f.key]??'—'}</b>}</div>)}</div>}</div></Page>}
