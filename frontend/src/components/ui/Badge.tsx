export default function Badge({value}:{value?:string}){const v=(value||'').toUpperCase();let c='badge-neutral';if(['AVAILABLE','COMPLETED','RECEIVED','ACTIVE','REPAIRED'].includes(v))c='badge-success';if(['IN_TRANSIT','ASSIGNED','UNDER_INSPECTION','UNDER_REPAIR','WAITING_FOR_PARTS','MEDIUM'].includes(v))c='badge-warning';if(['DAMAGED','LOST','DISPOSED','RETIRED','CLOSED','CRITICAL'].includes(v))c='badge-danger';return <span className={'badge '+c}>{value||'—'}</span>}
