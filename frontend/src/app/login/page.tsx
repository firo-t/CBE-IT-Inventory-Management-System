'use client';import {useState} from 'react';import {api} from '@/lib/api';import {Package,LogIn} from 'lucide-react';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [err,setErr]=useState('');const submit=async(e:any)=>{e.preventDefault();setBusy(true);setErr('');try{const r=await api.post('/auth/login',{email,password});localStorage.setItem('cbe_access_token',r.data.access_token);localStorage.setItem('cbe_user',JSON.stringify(r.data.user));location.href='/dashboard'}catch(x:any){setErr(x.response?.data?.message||'Login failed. Check your credentials and API server.')}finally{setBusy(false)}};return <div className="login"><div className="login-card"><div className="login-brand"><div className="logo"><Package size={21}/></div><h1 style={{margin:'12px 0 5px'}}>CBE Inventory</h1><div className="muted">Management System</div></div><form onSubmit={submit} style={{display:'grid',gap:15}}><div className="field"><label>Email</label><input required type="email" className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@cbe.com.et"/></div><div className="field"><label>Password</label><input required type="password" className="input" value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<div className="error">{err}</div>}<button disabled={busy} className="btn btn-primary" style={{justifyContent:'center',padding:12}}>{busy?'Signing in...':<><LogIn size={16}/> Sign in</>}</button></form><div className="footer-note">Secure JWT authentication · Backend: localhost:3001</div></div></div>}
