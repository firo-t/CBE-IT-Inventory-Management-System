'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import DashboardLayout from '@/components/layout/DashboardLayout';import Page from '@/components/ui/Page';import {api} from '@/lib/api';export default function Create(){const r=useRouter();const [f,setF]=useState({asset_id:'',problem_description:'',priority:'MEDIUM'});const [err,setErr]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();setBusy(true);try{await api.post('/maintenance/requests',f);r.push('/maintenance/requests')}catch(x:any){setErr(x.response?.data?.message||'Could not create request.')}finally{setBusy(false)}};return <DashboardLayout><Page title="Create Maintenance Request" description="Report an issue and start the maintenance workflow."><div className="card"><form onSubmit={submit} className="form-grid"><div className="field"><label>Asset ID *</label><input className="input" required value={f.asset_id} onChange={e=>setF({...f,asset_id:e.target.value})}/></div><div className="field"><label>Priority</label><select className="select" value={f.priority} onChange={e=>setF({...f,priority:e.target.value})}>{['LOW','MEDIUM','HIGH','CRITICAL'].map(x=><option key={x}>{x}</option>)}</select></div><div className="field span2"><label>Problem description *</label><textarea className="textarea" required rows={5} value={f.problem_description} onChange={e=>setF({...f,problem_description:e.target.value})}/></div>{err&&<div className="error span2">{err}</div>}<div className="actions span2"><button type="submit" disabled={busy} className="btn btn-primary">{busy?'Submitting...':'Submit Request'}</button><button type="button" className="btn btn-secondary" onClick={()=>r.back()}>Cancel</button></div></form></div></Page></DashboardLayout>}
