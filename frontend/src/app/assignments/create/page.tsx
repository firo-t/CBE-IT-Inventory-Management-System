'use client';import DashboardLayout from '@/components/layout/DashboardLayout';import FormShell from '@/components/ui/FormShell';export default function Create(){return <DashboardLayout><FormShell title="Assign Asset" description="Assign an AVAILABLE asset to an employee." endpoint="/assignments" fields={[{name:'asset_id',label:'Asset ID',required:true},{name:'employee_id',label:'Employee ID',required:true},{name:'employee_name',label:'Employee name',required:true},{name:'branch_id',label:'Branch ID',required:true},{name:'assigned_date',label:'Assigned date',type:'date'}]}/></DashboardLayout>}
