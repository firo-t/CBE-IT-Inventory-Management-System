'use client';import DashboardLayout from '@/components/layout/DashboardLayout';import FormShell from '@/components/ui/FormShell';export default function Create(){return <DashboardLayout><FormShell title="Create Dispatch" description="Move an asset to another branch." endpoint="/dispatches" fields={[{name:'asset_id',label:'Asset ID',required:true},{name:'source_location',label:'Source location',required:true},{name:'destination_branch_id',label:'Destination branch ID',required:true},{name:'receiver_name',label:'Receiver name',required:true},{name:'receiver_id',label:'Receiver ID',required:true},{name:'receiver_phone',label:'Receiver phone',required:true},{name:'dispatched_date',label:'Dispatch date',type:'date'}]}/></DashboardLayout>}
